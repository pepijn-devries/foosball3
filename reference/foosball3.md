# Start the Foosball App

Renders the Graphical User Interface and starts the server to interact
with a Foosball SQLite database.

## Usage

``` r
foosball3(...)
```

## Arguments

- ...:

  Ignored

## Value

Returns `NULL` invisibly.
